{
  "name": "astro-orpc",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler types && bunx --bun astro dev",
    "build": "wrangler types && bunx --bun astro build",
    "preview": "wrangler types && bunx wrangler dev",
    "astro": "bunx --bun astro",
    "wrangler": "bunx wrangler"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.5.3",
    "@orpc/client": "^1.3.0",
    "@orpc/server": "^1.3.0",
    "astro": "^5.8.0"
  },
  "devDependencies": {
    "cloudflare": "^4.3.0",
    "wrangler": "^4.17.0"
  }
}
